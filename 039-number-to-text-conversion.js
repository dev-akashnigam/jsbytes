const realNum = 6859;
const decimalNum = 635.2415;

const realNumString = String(realNum);
const decimalNumString = String(decimalNum);

console.log(`String corresponding to the real number ${realNum} = "${realNumString}"`);
console.log(`String corresponding to the decimal number ${decimalNum} = "${decimalNumString}"`);