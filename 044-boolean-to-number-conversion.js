const boolValue = true;

const numForBooleanValue = Number(boolValue); // In JS, this is the direct way to convert boolean values to numbers (0 or 1)

console.log(`Number corresponding to the boolean value: ${boolValue} = ${numForBooleanValue}`);